[project]
name = "my-app"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31.0",
    "flask==3.0.0",
    "click~=8.1.0",
    "pydantic[email]>=2.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "mypy==1.8.0",
]
docs = [
    "sphinx>=7.0",
]
