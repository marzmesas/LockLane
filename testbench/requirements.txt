# LockLane testbench – mixed outdated pins to exercise batch selection filters
# Includes major, minor, and patch-level gaps

# Major bumps (e.g. flask 2.x -> 3.x, pydantic 1.x -> 2.x)
flask==2.2.0
pydantic==1.10.0
numpy==1.24.0

# Minor bumps (one minor version behind latest)
requests==2.31.0
rich==13.8.0
uvicorn==0.31.0
httpx==0.27.0

# Patch bumps (same minor, one or two patches behind)
click==8.1.3
packaging==24.1
python-dotenv==1.0.0

# Mix
fastapi==0.111.0
pandas==2.2.0
